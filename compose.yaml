services:
  web:
    build:
      context: ./web
    ports:
      - "8082:80"
    depends_on:
      - api
    restart: unless-stopped

  api:
    build:
      context: ./api
      args:
        OB_WORKER_TAG: "0.1.6-alpha"
    environment:
      - AGREE_TO_MATLAB_RUNTIME_LICENSE=yes
      - OB_CACHE_DIR=/data/cache
      - OB_CACHE_IDLE_TIMEOUT=30
    restart: unless-stopped
    volumes:
      - ./media/cache:/data/cache
